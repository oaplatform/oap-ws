profiles = [
  http
  http-plain
]

services {
  oap-http-listener-plain.parameters.port = ${HTTP_PORT}

  session-manager.parameters.cookieDomain = localhost

  oap-ws-file-bucket-manager.parameters.buckets {
    DEFAULT = ${TMP_PATH}/default
    b1 = ${TMP_PATH}/b1
  }
}
