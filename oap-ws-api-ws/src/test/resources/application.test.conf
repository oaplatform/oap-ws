boot.main = oap-ws-api-ws-test

profile = http

services {
  oap {
    oap-http-server.parameters.port = ${TEST_HTTP_PORT}
    oap-http-health-handler.parameters.port = ${TEST_HTTP_PORT}
    oap-prometheus-metrics.parameters.port = ${TEST_HTTP_PORT}
  }

  oap-ws.session-manager.parameters.cookieDomain = "localhost"
}
