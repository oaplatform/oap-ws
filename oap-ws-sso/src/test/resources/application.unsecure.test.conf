profiles = [
  http-io
  plain-listener
]

services {
  plain-http-listener.parameters.port = ${TEST_HTTP_PORT}

  auth-ws.parameters.cookieDomain = localhost
  session-manager.parameters.cookieDomain = localhost
}
